<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"

              android:layout_width="match_parent"
              android:layout_height="100dp">

    <RelativeLayout
            android:id="@+id/pull_footer"
            android:padding="15dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

        <ProgressBar
                android:id="@+id/footer_progressbar"
                android:indeterminateDrawable="@drawable/godspeed_title_loading"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_centerInParent="true"/>

        <TextView
                android:id="@+id/footer_hint_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:visibility="invisible"
                android:text="加载更多"
                android:textColor="@color/black_color"/>

        <ImageView
                android:id="@+id/footer_arrow"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginRight="30dp"
                android:layout_toLeftOf="@id/footer_hint_text"
                android:contentDescription="@null"
                android:src="@drawable/godspeed_pull_arrow_up"
                android:visibility="gone"/>
    </RelativeLayout>

</LinearLayout>